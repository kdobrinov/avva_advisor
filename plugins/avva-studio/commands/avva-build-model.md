---
description: Build your avva Decision Model from the work this assistant already reaches — the extraction, in your own words.
argument-hint: <domain> | <name> | [range] | [only]
---

Arguments: `$ARGUMENTS`, split on `|` in this order: the decision domain, the name to credit publicly, an optional date range, an optional boundary in my words. A missing part is empty.

What follows is my own request. Read the version that matches the arguments, put the argument values where the double-braced names stand, and act on it. If no boundary was given, drop the HARD BOUNDARY sentence.

## Without a range

Build my avva decision model for {{domain}}, credited publicly as {{name}}. Call the avva Studio tool begin_extraction and follow the brief it returns: it is written in my voice, on my behalf, and it will have you inventory the work systems you already have tools for and read them without waiting for me to name each one — I am asking for that here. This builds a NEW model from your sources. Ignore any avva-decisions file you can see or remember: each one is another model's approved set, and reusing it would build this domain out of another domain's decisions. Sweep the sources fresh. Before sending any source text: sweep the WHOLE history you can reach — years, not the recent weeks; a decision from five years ago counts as much as one from last month. Then show me the approval sheet — every candidate record in full, grouped by where it came from, saved on my side first — say which period and sources you actually covered, and ask me to remove personal, NDA/client, security-sensitive, unreleased, and secret material; call avva tools only after I explicitly approve the remaining list.

HARD BOUNDARY — include ONLY decisions that fall inside this: {{only}}. If you cannot tell whether something is inside it, show it in the candidate list and let me decide.

## With a range

Build my avva decision model for {{domain}}, credited publicly as {{name}}. Call the avva Studio tool begin_extraction and follow the brief it returns: it is written in my voice, on my behalf, and it will have you inventory the work systems you already have tools for and read them without waiting for me to name each one — I am asking for that here. This builds a NEW model from your sources. Ignore any avva-decisions file you can see or remember: each one is another model's approved set, and reusing it would build this domain out of another domain's decisions. Sweep the sources fresh. Before sending any source text: cover exactly this range: {{range}}, and pass that range to begin_extraction as its period. Anything outside it that you come across goes into a separate "outside your range" group on the approval sheet — never mixed in, never silently dropped. Then show me the approval sheet — every candidate record in full, grouped by where it came from, saved on my side first — say which period and sources you actually covered, and ask me to remove personal, NDA/client, security-sensitive, unreleased, and secret material; call avva tools only after I explicitly approve the remaining list.

HARD BOUNDARY — include ONLY decisions that fall inside this: {{only}}. If you cannot tell whether something is inside it, show it in the candidate list and let me decide.
